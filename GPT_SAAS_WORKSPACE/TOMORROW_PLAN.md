# TOMORROW_PLAN

DATE WRITTEN: 2026-07-04 · For: next working session · Owner map in each line.

GPT SAAS v1 is verified (see `09_AUDIT/VERIFICATION_REPORT.md`). Tomorrow is about moving P001
from ACTIVE toward a real deliverable and hardening the registry.

## Priority order

1. **P001 — start the build (P0).**
   - Operator: drop current 247tiket / Travel Layout materials into `05_IMPORT_INBOX/` marked
     CURRENT (T1). If unavailable, Claude runs the Increment-0 fallback: fetch
     `claude/tenta-launch-setup-l4h420` (+ the two HTML-build branches), inventory files into
     `FILE_INVENTORY.md`, merge real source into `SOURCES/`.
   - Operator: confirm/correct GOAL + DONE_WHEN (D-5, T2).
   - Claude: produce the Increment-1 build plan (T3) from the consolidated sources.

2. **P002 — Marketing AI Growth OS (P1).** Verify the 6-language landing page renders; decide
   deploy-to-Vercel. Register as `06_PROJECTS/P002_Marketing_AI_Growth_OS/` with a PROJECT_STATE.

3. **P003 — Project 770 (P1).** Consolidate the two 770 branches (module + complete) into one
   project folder + single state.

4. **P005 — ClientFlow Agent Pack (P1).** Recover the CRM bulk-leads branch; define DONE_WHEN.

5. **Dormant sweep (P008–P013).** Operator exports the Sale/Scale XL, Broker, and Tools threads
   into `05_IMPORT_INBOX/`; ChatGPT classifies; registry entries upgraded from DORMANT.

## Standing rules for tomorrow

- Update `PROJECT_STATE.md` at the end of every session before anything else.
- Log blockers to `DECISION_REQUIRED.md` with a default; never stop the whole project.
- No final ZIP for any project until it passes REVIEW.
- Do not finish any website until its parent verification gate is green (GPT SAAS v1 already is).

## Open decisions to resolve (operator)

- **D-5:** confirm P001 GOAL + DONE_WHEN.
- **D-6:** provide P001 current materials / local paths.
- **D-7:** confirm whether Sale XL and Scale XL are one project or two (affects P008).
- **D-8:** confirm whether SDR Mini (P006) is standalone or folds into ClientFlow (P005).
