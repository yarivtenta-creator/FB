# DECISION_REQUIRED

Open blockers. Each has an **assumed default** so work never halts. Operator marks ANSWERED or
DEFAULT-ACCEPTED.

| ID | Question | Assumed default | Status |
|----|----------|-----------------|--------|
| D-5 | P001 GOAL + DONE_WHEN correct? | Production-ready Travel Layout, audit-passed, exported | DEFAULT-ACCEPTED |
| D-6 | P001 current materials / local paths? | Fetch from git candidate branches if not provided | DEFAULT-ACCEPTED |
| D-7 | Sale XL and Scale XL — one project or two? | Treat as one (P008) until told otherwise | DEFAULT-ACCEPTED |
| D-8 | SDR Mini standalone or part of ClientFlow? | Standalone (P006) until told otherwise | DEFAULT-ACCEPTED |

Resolving a decision → mark ANSWERED here, then the affected project auto-returns from BLOCKED
to ACTIVE. Nothing here blocks the rest of the system.
