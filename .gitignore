# Backups and logs are runtime artifacts, not source.
GPT-Memory/**/_backups/
GPT-Memory/_logs/

# Ingestion output is generated from the user's own data (copies of originals +
# machine-specific paths) — runtime, not repo source.
GPT-Memory/_RAW/
GPT-Memory/_INDEX/
GPT-Memory/_STAGING/
GPT-Memory/_AUDIT/
GPT-Memory/_PUBLISH/

# Windows / editor cruft
Thumbs.db
Desktop.ini
*~
