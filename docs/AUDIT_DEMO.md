# Deep Audit — first real test (proof)

One-project audit of a duplicate-idea cluster. Reproduce with:

```powershell
# Dry-run (report only — safe):
.\scripts\Invoke-DeepAudit.ps1 -Project "Skill Safety" -ModuleName "Skill Safety Check"

# Apply confident decisions (append-only overlay + module):
.\scripts\Invoke-DeepAudit.ps1 -Project "Skill Safety" -ModuleName "Skill Safety Check" -Apply

# Operator approves a queued candidate, then recompile:
.\scripts\Approve-AuditItem.ps1 -Id <queued-id>
.\scripts\Compile-ProjectMemory.ps1 -Project ALL
```

## Input (6 UNASSIGNED conversations from ChatGPT + Claude)

`Skill Antivirus idea` · `Skill Validator plan` · `Skill checker discussion` ·
`Pre-upload validation notes` · `Vinyl Lab deploy` · `Coffee and music musings`

## Audit result

```
primary cluster (title-stem clustering): 4 items — the "skill" family
  core (share dominant stem "skill"): Antivirus, Validator, Checker   -> merged
  candidate (weak link "valid"): Pre-upload validation notes          -> QUEUED for operator
routed by name match: Vinyl Lab deploy -> Vinyl Lab
uncertain: Coffee and music musings   -> UNCLASSIFIED (never forced)
```

Operator approved the queued candidate → 4 discussions merged into one module.

## Consolidated project state (generated by the Compiler)

```
# PROJECT INDEX — Skill Safety
- Items: 4  | Active conversations: 0 | Archived: 4 | Modules: 1

## Modules (consolidated — the active canonical record)
| Module | Status | Aliases | Members | Decision |
| Skill Safety Check | Not Built | Skill Antivirus idea; Skill Validator plan;
  Skill checker discussion; Pre-upload validation notes | 4 |
  keep consolidated state; archive duplicate discussions |

## Archived (duplicate discussions — cold history, not active memory)
  (the 4 raw transcripts, in _RAW, listed but NOT active)
```

## Decisions — append-only overlay (`_INDEX/assignments.ndjson`)

```json
{"id":"45e9c5351f81","project":"Skill Safety","status":"ARCHIVED","decided_by":"audit", ...}
{"id":"300f7ce7be6e","project":"Skill Safety","status":"ARCHIVED","decided_by":"audit", ...}
{"id":"04a67c101ff2","project":"Skill Safety","status":"ARCHIVED","decided_by":"audit", ...}
{"id":"45fca78c1a6f","project":"Vinyl Lab","status":"CLASSIFIED","decided_by":"audit", ...}
{"id":"46eb7cc0081a","project":"UNCLASSIFIED","status":"NEEDS_REVIEW","decided_by":"audit", ...}
{"id":"58eb2258f55c","project":"Skill Safety","status":"ARCHIVED","decided_by":"operator", ...}
```

## Success criteria — all met

- ✅ One project (Skill Safety) has a clean consolidated state: **one module**, 0 active conversations.
- ✅ Duplicate cluster **merged** (3 confident) **and** the weak one **queued for Operator approval** → approved.
- ✅ Uncertain item **parked in UNCLASSIFIED** (never forced into a project).
- ✅ **No raw history is active memory** — the 4 transcripts are ARCHIVED cold history; only the module is active.
- ✅ **All generated outputs come only from the Compiler** (PROJECT_INDEX is "generated — do not hand-edit").
- ✅ **All decisions via the append-only overlay** — the master index was never modified.
- ✅ Item status detected (all "remaining" → module **Not Built**; negation "not built yet" handled).
