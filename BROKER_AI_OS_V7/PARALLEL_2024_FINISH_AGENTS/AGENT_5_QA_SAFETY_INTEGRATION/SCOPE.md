# AGENT 5 — SCOPE

## In scope
- `data_layer/routes.js` (merge route stubs)
- `server.js` (mount new routers only — minimal, additive)
- `tests/run.js`, `tests/endpoints.js`
- `FINAL_PROOF_PACKAGE/**`

## Out of scope
- Re-implementing module logic (consume Agents 1–4 outputs)
- Any change to `3023`, `2025`, `/home/user/FB`, staging — FORBIDDEN

## Authority
Agent 5 is the ONLY agent allowed to edit `server.js` and `data_layer/routes.js`, to prevent
multi-agent collisions on shared files.
</content>
