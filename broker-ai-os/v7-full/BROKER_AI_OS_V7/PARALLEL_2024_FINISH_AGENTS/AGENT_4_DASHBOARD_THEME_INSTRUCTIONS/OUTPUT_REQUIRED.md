# AGENT 4 — OUTPUT REQUIRED (handoff)

## Endpoints consumed (must exist post-merge)
- `GET /api/data/providers`, `POST /api/data/providers/:id/test`
- `GET /api/data/paper/candidates`, `GET /api/data/paper/trades`, `GET /api/data/paper/stats`
- `GET /api/data/hub/health`

## Evidence to hand Agent 5
- Screenshot/snapshot of dashboard in BOTH themes (or documented start instructions if no browser).
- Confirmation that no key-capturing input posts to the server.
- List of operator guide files created.
</content>
