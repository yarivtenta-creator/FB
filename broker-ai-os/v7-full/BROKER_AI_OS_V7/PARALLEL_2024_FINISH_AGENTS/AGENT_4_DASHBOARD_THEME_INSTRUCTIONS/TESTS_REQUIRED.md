# AGENT 4 — TESTS REQUIRED (static/structural; UI verified via running server)

- `public/index.html` contains a theme toggle element and references `theme.js`.
- `public/index.html` contains a Provider Setup Center section and a Paper Status section.
- `public/index.html` contains NO `type="password"` or key-submitting form posting to a non-test endpoint.
- `public/theme.js` defines both `light` and `dark` and reads/writes `localStorage`.
- All six operator guide files exist and are non-empty.
</content>
